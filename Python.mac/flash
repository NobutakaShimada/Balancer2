#!/bin/sh
set -eu

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
PYTHON=${PYTHON:-python}

if [ "$#" -lt 1 ]; then
    echo "usage: ./flash firmware.bin [flash_lpc13xx_iflash.py options]" >&2
    exit 2
fi

exec sudo "$PYTHON" "$SCRIPT_DIR/tools/flash_lpc13xx_iflash.py" "$@" --yes
